apiVersion: v1
kind: Service
metadata:
  name: node-app
spec:
  selector:
    app: node-app
  ports:
  - protocol: TCP
    port: 3001
    targetPort: 3001
  type: LoadBalancer # Or NodePort if you don’t have a cloud LB
